
import React from 'react';

interface ErrorAlertProps {
  show: boolean;
}

// Component is now empty - won't show errors
const ErrorAlert: React.FC<ErrorAlertProps> = ({ show }) => {
  return null;
};

export default ErrorAlert;
